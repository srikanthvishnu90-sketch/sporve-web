# robin run — 2026-09-08 (Codex work audit, five read-only surfaces + typecheck backstop)

Owner ask: "have robin audit codex work, make sure it doesn't steer off task
and keeps going." Five parallel read-only auditors + `deno check` + live
probes. Nothing was edited by the auditors.

## Verdict in one screen

| surface | verdict | what is open |
|---|---|---|
| 1 onboarding (`stripe-connect-onboarding`) | **CORRECT** | operational only: confirm the deployed `CONNECT_RETURN_ORIGINS` secret lists `https://sporv.ai` (no side-effect-free probe exists; the function creates the Stripe account before it validates the return URL) |
| 2 checkout (`stripe-create-checkout`, `installment-checkout`) | **CORRECT** | `CHECKOUT_ORIGINS` verified LIVE: sporv.ai accepted (404 booking-not-found), evil.example refused (400). Latents: installment payer lookup fails CLOSED when a member has two payer guardians; installment idempotency key is fixed per installment (dead session URL on a retry inside 24h) |
| 3 webhook (`stripe-webhook`) | **RED — one live defect** | see below. Otherwise GREEN on signature, Connect scoping, service-role RPCs, event coverage; YELLOW: mode-mismatch returns 500 (noisy retries, never applied), no `config.toml` pin for `--no-verify-jwt` (live setting verified earlier: webhook is deployed without JWT verification) |
| 4 refund / payouts | **CORRECT on money mechanics** | `refund_requests` state machine is never consulted (parent self-serves a full refund; no partial top-up possible); payouts echoes a raw DB error at `:79` |
| 5 scope + stagnation | **ON TASK / STALLED** | #358 touched 58 files: every one is in-scope or security-adjacent and harmless (largest off-task item is Codex's own 1.9k-line ledger doc). Codex applied nothing RED itself. **No Codex output since 2026-09-06 17:04.** A ledger claim opened tonight ("six-prompt launch intake, Prompt 1 billing") with zero files behind it. |
| typecheck backstop | no logic errors | `apiVersion: "2024-06-20"` does not match the pinned SDK `npm:stripe@14.21.0` types (runtime accepts it; a future SDK bump will flip behaviour silently — pin both together); untyped supabase-js clients produce `never` noise |

## The RED defect (mine, not Codex's)

`20260907_001032_ledger_append_only.sql` — applied 2026-09-07 by me under "apply
the red drafts" — installed a trigger that raises on **every** update of
`payment_event_ledger`. Both money RPCs claim the event first
(`insert … outcome='ignored'`) and promote it to `'applied'` after the booking
or subscription update. Live in prod today: `trg_ledger_append_only` enabled,
`apply_stripe_booking_event` (2 overloads) and `apply_stripe_billing_event`
still run that promotion. **Any Stripe payment event would raise, roll the
booking back to unpaid, return 500, and retry for 3 days.** No customer hit it:
last ledger event 2026-09-01, `webhook_dead_letter` has 0 rows, `bookings` is
empty.

Proof (rolled-back DO block, prod, 2026-09-08): today's trigger → promotion
RAISED 55000; proposed trigger → promotion ALLOWED, money-column edit RAISED,
delete RAISED.

Fix staged: `docs/red-drafts/2026-09-08-ledger-promotion-fix.sql` — the trigger
permits exactly one transition (`ignored → applied`, every other column
unchanged). Owner applies with "apply the ledger fix". Until then the payment
path is closed; do not attempt the first real charge.

## Codex: stalled, and what it needs to keep going

Codex stayed on task in its last delivered batch; the problem is throughput,
not drift. Nothing observable since 09-06. Tomorrow's prompt is queued
(`docs/codex-prompts/2026-09-08-stripe-fifth-state.md`, embedded in
`docs/owner-list-2026-09-09.md` §F). Relay these with it:

1. Stripe test secret + Connect webhook signing secret (confirm still valid).
2. Stripe → Developers → Webhooks → Connect endpoint → add `account.updated`,
   `account.application.deauthorized`; give Codex the endpoint id.
3. A connected test account id (`acct_…`) for the deauthorization trigger test.
4. `stripe login` on Codex's machine for `stripe trigger`.
5. A working local Chromium for `bash src/smoke.sh` (what blocked its last release).
6. Tell Codex the webhook pre-guard rejects unknown `event.account` with a 500 —
   its `account.updated` handler must run before that guard or the write-back
   must be guaranteed first.

Re-check the ledger claim in an hour; if no `end` and no files, it is a dead claim.

## Smaller items for the next Codex/Claude pass

- webhook: mode mismatch → return 400/ack, not 500; add `supabase/config.toml`
  pinning `[functions.stripe-webhook] verify_jwt = false`.
- refund: consult/advance `refund_requests`; allow partial top-ups.
- payouts `:79`: replace `provErr.message` with a fixed string.
- installment-checkout: filter the payer link by `guardians.user_id`; persist the
  session id for reuse.
- pin `apiVersion` to the SDK's own version string, or bump the SDK deliberately.
