# Constitution adoption and launch-driver contradictions

Updated2026-09-10. Requirements are not implementation evidence.

## Resolved by later owner specifications

- Keys free/solo/organization and labels Free/Solo/Organization replace the
  original Individual/Enterprise labels. Baseline pro/enterprise are legacy
  aliases requiring migration with compatible callers.
- Solo49/490 and Organization from199/1990 replace39/390 and449/4490.
  The September10 owner-confirmation record is in the current launch audit.
  Athlete-band/seat price confirmation is not inferred from base prices.
- Paid sends are fair-use unlimited; Organization includes150 members/5 admins
  and2500 Ask before metering, replacing the old unlimited Enterprise caps.
- No camp add-on at launch; two paid pricing cards and "two months free".
- Cancellation retains paid-period access; failed payment drops after dunning.
- The current owner requests all six prompts sequentially in one session,
  superseding the earlier one-prompt-per-session scheduling.
- STATE.md is the new progress authority. GATES.md defines business gates;
  neither commit volume nor the state file changes gate truth.
- The owner approved isolated GitHub draft PRs and PR-comment coordination
  because local terminal/Clo hooks are unavailable. This does not waive
  critical-path review or production verification.
- Source is sporve-web; sporve-agent-clone is the sporv.ai deployment mirror
  per CONTEXT.md and tools/deploy-prod.sh. Old AGENTS project/alias names
  must not redirect a release to the wrong site.

## Unresolved; do not silently credit either side

1. CONTEXT6.4/original Prompt1 require hard draft-generation limits/402;
   CONTEXT6.5/Prompt5 require actual overflow drafts to keep generating.
   Retain both requirements. Their final boundary/HTTP behavior needs explicit
   resolution before P1.03/P5.01 pass.
2. Literal I1 forbids every edge function from writing sent_at or moving money,
   while human-approved delivery/payments need trusted adapters. Existing work
   keeps generators unable to approve/send and requires immutable human
   approval at delivery; this does not satisfy the literal blanket grep.
3. The latest message ends at "instead pick" and omits its STATE template and
   remaining parking rules. Its opening permits green OR explicitly parked,
   but its loop marks DONE only when every criterion is[x]. For now PARKED
   remains non-green, not acceptance or permission to ship; no prompt is DONE
   on parked evidence. Continue independently actionable criteria after a
   concrete blocker is parked, without changing risk acceptance.
4. GATES.md's marketplace-oriented G3 differs from the B2B-only constitution.
   Do not revive consumer marketplace work to satisfy older wording.
5. The prior ingestion brief permits only email/bank permanent connectors and
   deterministic transport; CONTEXT includes other ongoing connectors/AI
   parsing. The ingestion Phase0 audit-only approval boundary remains;
   restoring launch requirements does not approve ingestion Phase1 feature code.

## Recovery record

STATE.md and PROMPTS.md returned GitHub404 on main. STATE was created and
committed first; PROMPTS recovers the available owner requirements with these
overrides, not missing text. No missing instruction is treated as permission
for production changes, new charges, deletion or waived launch checks.
